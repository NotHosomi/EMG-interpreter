+--------+
  README
+--------+

The utility scripts use absolute directory addresses, so these must be updated to be run
Conveniently, these variables are at the top of each script.

For the graphing scripts to work, new directories must be created in this folder

/log_graphs/
/sample_graphs/
/sequence_log_graphs/train_seqs/
/sequence_log_graphs/test_seqs/